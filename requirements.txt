# Core dependencies for Cracked.sh Recon Suite - Web Scraping Project
# Based on latest stable versions as of January 2026

# Scrapy framework for web crawling and scraping
scrapy>=2.14.0,<3.0.0

# Core dependencies that Scrapy relies on
lxml>=4.6.4
parsel>=1.10.0
w3lib>=2.3.1
twisted>=25.5.0
cryptography>=46.0.4
pyOpenSSL>=25.3.0

# Browser automation and anti-detection
camoufox>=0.4.11
playwright>=1.51.0

# Additional web scraping utilities
requests>=2.32.3
beautifulsoup4>=4.12.3
html5lib>=1.1

# Common utilities for data processing
itemadapter>=0.9.0
queuelib>=1.7.0

# Optional but recommended dependencies
pillow>=10.2.0  # For image processing in Scrapy
service-identity>=24.1.0  # For SSL certificate verification
zope.interface>=7.0.0  # For interface definitions